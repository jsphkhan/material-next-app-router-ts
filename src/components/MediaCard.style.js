const styles = (theme) => {
  return {
    testWrap: {
      display: 'flex',
      background: 'yellow',
      justifyContent: 'center',
      alignItems: 'center',
      padding: 2
    }
  };
};

export default styles;
